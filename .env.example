# Hotel Dashboard Environment Configuration

# Data source selection
SOURCE_TYPE=local_server  # Options: oracle_cloud, local_server
DATA_SOURCE=csv  # Options: postgres, csv

# Storage configuration
STORAGE_TYPE=local  # Options: local, s3
STORAGE_BUCKET=...  # Required if STORAGE_TYPE=s3
STORAGE_PREFIX=hotel_data
LOCAL_STORAGE_PATH=./storage

# Oracle Cloud connector (required if SOURCE_TYPE=oracle_cloud)
OPERA_BASE_URL=https://...
OPERA_CLIENT_ID=...
OPERA_CLIENT_SECRET=...
OPERA_APP_KEY=...
OPERA_HOTEL_ID=...

# PostgreSQL connector (required if DATA_SOURCE=postgres)
LOCAL_DB_HOST=localhost
LOCAL_DB_PORT=5432
LOCAL_DB_NAME=hotel_db
LOCAL_DB_USER=...
LOCAL_DB_PASSWORD=...

# CSV/Excel connector (required if DATA_SOURCE=csv)
LOCAL_CSV_PATH=./data/files
